defaults:
  - _self_
hydra:
  run:
    dir: ${logdir}
_target_: agent.finetune.lfgpo.train_lfgpo_diffusion_agent.TrainLFGPODiffusionAgent

name: ${env_name}_lfgpo_diffusion_mlp_ta${horizon_steps}_td${denoising_steps}
logdir: ${oc.env:DPPO_LOG_DIR}/robomimic/finetune/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}_${seed}
base_policy_path: ${oc.env:DPPO_LOG_DIR}/robomimic/pretrain/can/can_pre_diffusion_mlp_ta4_td20/2024-06-28_13-29-54/checkpoint/state_8000.pt
robomimic_env_cfg_path: cfg/robomimic/env_meta/${env_name}.json
normalization_path: ${oc.env:DPPO_DATA_DIR}/robomimic/${env_name}/normalization.npz

seed: 42
device: cuda:0
env_name: can
obs_dim: 23
action_dim: 7
denoising_steps: 20
cond_steps: 1
horizon_steps: 4
act_steps: 4

env:
  n_envs: 50
  name: ${env_name}
  best_reward_threshold_for_success: 1
  max_episode_steps: 300
  save_video: false
  wrappers:
    robomimic_lowdim:
      normalization_path: ${normalization_path}
      low_dim_keys: ['robot0_eef_pos',
                    'robot0_eef_quat',
                    'robot0_gripper_qpos',
                    'object']
    multi_step:
      n_obs_steps: ${cond_steps}
      n_action_steps: ${act_steps}
      max_episode_steps: ${env.max_episode_steps}
      reset_within_step: true

wandb:
  entity: ${oc.env:DPPO_WANDB_ENTITY}
  project: robomimic-${env_name}-finetune
  run: ${now:%H-%M-%S}_${name}

train:
  n_train_itr: 151
  n_critic_warmup_itr: 5
  n_steps: 300
  gamma: 0.999
  # actor (diffusion) / critic (twin Q) / ratio net optimizers
  actor_lr: 1e-5
  actor_weight_decay: 0
  actor_lr_scheduler:
    first_cycle_steps: 1000
    warmup_steps: 10
    min_lr: 1e-5
  critic_lr: 1e-3
  critic_weight_decay: 0
  critic_lr_scheduler:
    first_cycle_steps: 1000
    warmup_steps: 10
    min_lr: 1e-3
  ratio_lr: 3e-4          # lfgpo.py default
  ratio_weight_decay: 0
  save_model_freq: 100
  val_freq: 10
  render:
    freq: 1
    num: 0
  # --- off-policy replay (robomimic-side, from QSM) ---
  scale_reward_factor: 1  # lfgpo MuJoCo used ~0.5; robomimic sparse reward -> RETUNE
  critic_tau: 0.005
  policy_tau: 0.005         # Polyak for target policy (a' sampling)
  buffer_size: 5000         # * n_envs
  replay_ratio: 16
  batch_size: 1000
  ratio_updates_per_batch: 1

model:
  _target_: model.diffusion.diffusion_lfgpo.LFGPODiffusion
  # --- LFGPO-specific (lfgpo.py defaults) ---
  ppo_eps: 0.2
  max_ratio_weight: 5.0
  ratio_reg_lambda: 0.01
  adv_norm: true
  use_target_policy: true
  # Sampling HPs
  min_sampling_denoising_std: 0.1
  randn_clip_value: 3
  network_path: ${base_policy_path}
  actor:
    _target_: model.diffusion.mlp_diffusion.DiffusionMLP
    time_dim: 16
    mlp_dims: [512, 512, 512]
    residual_style: true
    cond_dim: ${eval:'${obs_dim} * ${cond_steps}'}
    horizon_steps: ${horizon_steps}
    action_dim: ${action_dim}
  critic:
    _target_: model.common.critic.CriticObsAct
    action_dim: ${action_dim}
    action_steps: ${act_steps}
    cond_dim: ${eval:'${obs_dim} * ${cond_steps}'}
    mlp_dims: [256, 256, 256]
    activation_type: Mish
    residual_style: true
  ratio_net:
    _target_: model.diffusion.diffusion_lfgpo.RatioNet
    obs_dim: ${obs_dim}
    action_dim: ${action_dim}
    cond_steps: ${cond_steps}
    horizon_steps: ${horizon_steps}
    hidden_sizes: [256, 256, 256]
  horizon_steps: ${horizon_steps}
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  denoising_steps: ${denoising_steps}
  device: ${device}
